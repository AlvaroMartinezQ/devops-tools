key pair name:
my-super-secret



packer validate .\example-ami.json

packer build -var-file="variables.json" .\example-ami.json

ssh -i .\my-super-secret.pem ubuntu@ip


### Removed commands from example-ami.json

"sudo npm install pm2@latest -g",
"sudo pm2 start ~/server.js",
"sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u ubuntu --hp /home/ubuntu",
"sudo pm2 save",
"sudo systemctl start pm2-ubuntu",
"sudo cp /root/.pm2/dump.pm2 /home/ubuntu/.pm2/dump.pm2"
